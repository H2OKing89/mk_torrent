[tool:pytest]
markers =
    integration: tests that hit filesystem or external deps
    slow: tests that take a long time to run
    skip_ci: tests to skip in CI environment

addopts =
    --strict-markers
    --strict-config
    -v

filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
